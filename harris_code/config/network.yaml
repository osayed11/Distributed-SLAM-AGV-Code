robots:
  - id: 0
    ip: "192.168.1.44"        # robot IP (myAGV)
    pub_port: 5551            # robot publishes its /odom-derived pose here
    mocap_rigid_id: 1         # PhaseSpace rigid body ID (only used if mocap_pub runs)

laptop:
  ip: "192.168.1.142"         # laptop IP (the machine running this GUI)
  mocap_pub_port: 5560        # mocap_pub.py publishes here (optional)
  goal_pub_port: 5562         # control_panel publishes goal / estop / ctrl_stop

control:
  frequency_hz: 20
